import React from 'react';
import {BottomNavigationAction, BottomNavigation, Typography } from '@mui/material'

const Footer = () => {
    return (  
        <React.Fragment>
        <Typography variant='h3'>this is the footer of the wbsite</Typography>
        </React.Fragment>
    );
}
 
export default Footer;